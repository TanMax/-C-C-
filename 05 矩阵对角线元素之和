#include <iostream> 
#include <stdio.h>
#include <math.h>
using namespace std;
int main()
{
	int a[3][3];
	int sum = 0;
	for (int i = 0; i < 3; i++)
	{
		for (int j = 0; j < 3; j++)
		{
			scanf_s("%d", &a[i][j]);
			if (i == j) sum = sum + a[i][j];
		}
	}
	printf("对角元素之和为：%d", sum);
}
